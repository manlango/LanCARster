<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>




    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">

  <div class="field">
    <p class ="text-box"> <%= form.label :role %>
      <%= form.select :role, options_for_select(["admin","agent", "sales", "finance", "inventory"]), { include_blank: false }, {class:"fields" }%>

    <%= form.submit 'Submit', :class => 'submit'%><br/>
        <%= link_to 'Back', users_path,class:"submit"  %></p>
  </div>  </p>



  </div>
<% end %>

  

